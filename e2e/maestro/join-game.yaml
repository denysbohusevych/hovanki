# Joins the game whose code the launch options prefilled.
appId: ${APP_ID}
---
- extendedWaitUntil:
    visible:
      id: home_join
    timeout: 30000
- tapOn:
    id: home_join
- runFlow:
    file: allow-location.yaml
    env:
      UNTIL: lobby_screen
